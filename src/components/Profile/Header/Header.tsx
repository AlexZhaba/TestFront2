import React from 'react';

import s from './Header.module.scss';

let Header: React.FC = () => {
    return (
        <header className={s.header}>
            DepConf
        </header>
    )
}

export {Header};